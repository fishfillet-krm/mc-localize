{
  "projectName": "mc-localize",
  "defaultBranch": "main",
  "packageManager": "unknown",
  "commands": {
    "install": "python -m pip install -e .",
    "build": "",
    "test": "PYTHONPATH=src python -m unittest discover -s tests",
    "lint": "",
    "format": ""
  },
  "agent": {
    "name": "Codex",
    "requirePlanBeforeChanges": true,
    "allowAutoPr": false,
    "protectedAreas": [
      "authentication",
      "authorization",
      "release",
      "deployment",
      "security",
      "billing"
    ]
  }
}
